/**
 * Drizzle schema. Populated per phase:
 *   Phase 1 (dashboard): posts, categories, tags, posts_tags, media, resumes, links, login_attempts
 * Kept intentionally empty in Phase 0 so `db:generate` produces a no-op migration
 * until we actually model an entity.
 */
export {};
