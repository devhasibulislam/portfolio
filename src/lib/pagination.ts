/**
 * Card-list pagination sizing for every public listing surface. Kept in one
 * place so /blog, /projects, and any future list share the same UX.
 */
export const PAGE_INITIAL = 6;
export const PAGE_NEXT = 3;
